{
  "name": "floatline-contact-form",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "An accessible floating-label contact form demo with zero runtime dependencies.",
  "scripts": {
    "test": "node --test",
    "check:syntax": "node --check assets/js/form.js && node --check assets/js/apps.js && node --check tests/form.test.js && node --check tests/markup.test.js",
    "check": "npm run check:syntax && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
